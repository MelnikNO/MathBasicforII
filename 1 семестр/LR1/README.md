# Отчет по лабораторной работе №1
## Логистическая регрессия для прогнозирования оттока клиентов банка


## Цель работы

Построить модель логистической регрессии для прогнозирования оттока клиентов банка, оценить качество модели и проинтерпретировать результаты для разработки кампании лояльности.

**Задачи:**
1. Подготовить данные для моделирования
2. Построить модель логистической регрессии
3. Оценить качество модели с помощью метрик и визуализации
4. Проанализировать и интерпретировать результаты
5. Дать практические рекомендации банку

---

## Используемые библиотеки

```python
import pandas as pd
import numpy as np
import matplotlib.pyplot as plt
import seaborn as sns

from sklearn.model_selection import train_test_split
from sklearn.preprocessing import StandardScaler
from sklearn.linear_model import LogisticRegression
from sklearn.metrics import (
    accuracy_score, precision_score, recall_score,
    f1_score, roc_auc_score, confusion_matrix,
    classification_report, roc_curve
)

import warnings
warnings.filterwarnings('ignore')
```

---

## Генерация и анализ данных

### 1. Генерация синтетических данных

Поскольку исходный файл `Churn_Modelling.csv` отсутствовал, были сгенерированы синтетические данные, моделирующие поведение клиентов банка.

**Характеристики данных:**
- Количество записей: 10 000
- Количество признаков: 14
- Целевая переменная: `Exited` (отток клиента)

**Признаки:**
- `RowNumber` — номер строки
- `CustomerId` — идентификатор клиента
- `Surname` — фамилия
- `CreditScore` — кредитный рейтинг
- `Geography` — страна
- `Gender` — пол
- `Age` — возраст
- `Tenure` — стаж в банке
- `Balance` — баланс
- `NumOfProducts` — количество продуктов
- `HasCrCard` — наличие кредитной карты
- `IsActiveMember` — активность
- `EstimatedSalary` — предполагаемая зарплата
- `Exited` — целевая переменная

### 2. Распределение целевой переменной

```python
df['Exited'].value_counts()
```

**Результаты:**

| Статус | Количество | Доля |
|--------|------------|------|
| Ушедшие (1) | 6,675 | 66.75% |
| Лояльные (0) | 3,325 | 33.25% |

<img width="846" height="721" alt="image" src="https://github.com/user-attachments/assets/afa183e8-1cb0-414f-aa8b-ab259c77e64c" />

<img width="1394" height="836" alt="image" src="https://github.com/user-attachments/assets/c85e9047-85f3-4c5a-aa4b-341813ba4c8c" />


### 3. Анализ категориальных признаков

**Распределение по странам:**

| Страна | Количество | Доля оттока |
|--------|------------|-------------|
| France | 5,014 | 16.2% |
| Spain | 2,477 | 16.8% |
| Germany | 2,509 | 32.4% |

**Распределение по полу:**

| Пол | Количество | Доля оттока |
|-----|------------|-------------|
| Female | 4,543 | 25.1% |
| Male | 5,457 | 16.5% |

<img width="1344" height="499" alt="image" src="https://github.com/user-attachments/assets/f13bb2e3-2f4c-4a49-954e-a9a456859deb" />

<img width="1342" height="495" alt="image" src="https://github.com/user-attachments/assets/399dcbfd-8c3f-4ab4-927e-a291f9656eb2" />


**Выводы:**
- Клиенты из Германии имеют самый высокий уровень оттока (32.4%)
- Женщины уходят чаще мужчин (25.1% против 16.5%)

### 4. Анализ числовых признаков

<img width="1346" height="450" alt="image" src="https://github.com/user-attachments/assets/6b72439c-e30c-4a95-b3ce-756c44dd3351" />

<img width="1344" height="451" alt="image" src="https://github.com/user-attachments/assets/bc18ce25-a359-41ba-b59a-31d3ec53f70d" />

<img width="1356" height="436" alt="image" src="https://github.com/user-attachments/assets/9bd8aff6-c036-482e-aeeb-5a642f1f8acb" />



### 5. Корреляционный анализ

<img width="1106" height="998" alt="image" src="https://github.com/user-attachments/assets/ee278b8d-b18e-4be9-a309-e36be43ddaa9" />


<img width="398" height="361" alt="image" src="https://github.com/user-attachments/assets/2e851770-60df-4bd8-ad8d-ff2554201e2a" />


---

## Предобработка данных

### 1. Удаление неинформативных признаков

```python
df_clean = df.drop(['RowNumber', 'CustomerId', 'Surname'], axis=1)
```

### 2. Кодирование категориальных признаков

```python
df_clean['Gender'] = df_clean['Gender'].map({'Female': 0, 'Male': 1})
df_encoded = pd.get_dummies(df_clean, columns=['Geography'])
```

**Признаки после кодирования:**
```
['CreditScore', 'Age', 'Tenure', 'Balance', 'NumOfProducts', 
 'HasCrCard', 'IsActiveMember', 'EstimatedSalary', 'Exited',
 'Geography_France', 'Geography_Germany', 'Geography_Spain', 'Gender_1']
```

### 3. Масштабирование и разделение данных

```python
scaler = StandardScaler()
X_scaled = scaler.fit_transform(X)

X_train, X_test, y_train, y_test = train_test_split(
    X_scaled, y, test_size=0.2, random_state=42, stratify=y
)
```

**Результаты разделения:**
- Обучающая выборка: 8,000 записей (80%)
- Тестовая выборка: 2,000 записей (20%)

---

## Построение модели

### Обучение модели

```python
model = LogisticRegression(
    random_state=42, 
    max_iter=1000, 
    class_weight='balanced'
)
model.fit(X_train, y_train)
```

**Параметры модели:**
- `class_weight='balanced'` — автоматическая балансировка классов
- `max_iter=1000` — достаточное количество итераций для сходимости

### Прогнозирование

```python
y_pred = model.predict(X_test)
y_pred_proba = model.predict_proba(X_test)[:, 1]
```

---

## Оценка качества модели

### 1. Метрики классификации

| Метрика | Значение | Интерпретация |
|---------|----------|---------------|
| **Accuracy** | 0.6450 (64.50%) | Доля правильных предсказаний |
| **Precision** | 0.7940 (79.40%) | Точность для класса "ушедшие" |
| **Recall** | 0.6322 (63.22%) | Полнота для класса "ушедшие" |
| **F1-score** | 0.7039 (70.39%) | Гармоническое среднее |
| **ROC-AUC** | 0.7045 (70.45%) | Площадь под ROC-кривой |

### 2. Матрица ошибок

| | Предсказано: Лояльные | Предсказано: Ушедшие |
|---|---|---|
| **Фактически: Лояльные** | 446 (67.1%) | 219 (32.9%) |
| **Фактически: Ушедшие** | 491 (36.8%) | 844 (63.2%) |

<img width="1333" height="489" alt="image" src="https://github.com/user-attachments/assets/00e91519-418f-4998-815f-1a8d724a8552" />


**Интерпретация:**
- **True Negative (TN):** 446 — правильно предсказаны лояльные клиенты
- **False Positive (FP):** 219 — лояльные ошибочно отнесены к ушедшим
- **False Negative (FN):** 491 — ушедшие клиенты не выявлены (пропущены)
- **True Positive (TP):** 844 — правильно выявлены ушедшие клиенты

### 3. ROC-кривая

<img width="1389" height="490" alt="image" src="https://github.com/user-attachments/assets/664dd071-18d4-4c59-bcc4-a02c984e21ef" />


**Интерпретация:**
- AUC = 0.7045 — модель имеет приемлемое качество (AUC > 0.7)
- Модель значительно лучше случайного классификатора

---

## Интерпретация результатов

### 1. Анализ коэффициентов модели

<img width="1189" height="790" alt="image" src="https://github.com/user-attachments/assets/4e302ef8-ed83-4ca3-9440-4afc89d1d3ef" />


**Факторы риска (увеличивают вероятность оттока):**

| Признак | Коэффициент | Влияние |
|---------|-------------|---------|
| Age (Возраст) | +0.238 | 🔴 Увеличивает риск |
| Balance (Баланс) | +0.179 | 🔴 Увеличивает риск |
| NumOfProducts | +0.109 | 🔴 Увеличивает риск |

**Защитные факторы (снижают вероятность оттока):**

| Признак | Коэффициент | Влияние |
|---------|-------------|---------|
| CreditScore | -0.674 | 🟢 Снижает риск |
| IsActiveMember | -0.142 | 🟢 Снижает риск |

### 2. Практические рекомендации банку

1. **Разработать программы для возрастных клиентов (40+)** — возраст является самым сильным фактором риска
2. **Поощрять активность клиентов** — активные клиенты значительно реже уходят
3. **Анализировать причины оттока клиентов с 3+ продуктами** — парадоксально, но больше продуктов = выше риск
4. **Улучшить работу с клиентами из Германии** — высокий уровень оттока в этом регионе
5. **Создать персональные предложения для мужчин** — мужчины уходят чаще женщин

---

## Выводы

### 1. Основные результаты

| Параметр | Значение |
|----------|----------|
| Количество записей | 10,000 |
| Использовано признаков | 12 |
| Accuracy | 64.50% |
| Recall | 63.22% |
| ROC-AUC | 70.45% |

### 2. Ключевые выводы

1. **Факторы риска оттока:**
   - Возраст клиента (самый сильный фактор)
   - Высокий баланс на счете
   - Количество используемых продуктов

2. **Защитные факторы:**
   - Высокий кредитный рейтинг (сильно снижает риск)
   - Статус активного клиента

3. **Качество модели:**
   - Модель демонстрирует приемлемое качество (ROC-AUC > 0.7)
   - Хорошо находит ушедших клиентов (recall > 63%)
   - Требуется улучшение точности (precision)

### 3. Направления улучшения модели

1. **Борьба с дисбалансом классов:**
   - SMOTE (синтетическая генерация выборки)
   - Подвыборка большинства
   - Ансамблевые методы

2. **Другие модели:**
   - Random Forest
   - XGBoost
   - Gradient Boosting

3. **Новые признаки:**
   - Отношение баланса к зарплате
   - Возрастные группы
   - Интеракции признаков

4. **Настройка гиперпараметров:**
   - GridSearchCV
   - RandomizedSearchCV

